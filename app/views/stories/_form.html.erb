<%= quill({modules: []}, style: 'height:35em') do %>
    <%= @story.body.html_safe %>
<% end %>

<%= simple_form_for @story do |f| %>
    <%= quill_field f, :body %>
    <%= f.submit 'Submit', class: 'btn btn-large btn-primary mt-2' %>
<% end %>

